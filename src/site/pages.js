import { modules, roles, policies } from './content.js';
import { icon, escapeHtml } from './utils.js';

export const cta = () => `<section class="cta-band"><div class="wrap cta-inner"><div><p class="eyebrow">BUILD A BUSINESS THAT GIVES YOU ROOM TO LIVE</p><h2>Leave the site at the site.</h2></div><div class="actions"><a class="button primary" href="/contact">Book a demo ${icon('arrow-up-right')}</a><a class="button" href="/pricing">Configure your pricing ${icon('arrow-right')}</a></div></div></section>`;
export function homePage() {
  return `<section class="scroll-intro" aria-label="Construction story"><section class="hero" aria-label="Site Killick construction operations"><div id="site-scene" aria-hidden="true"></div><div class="hero-copy"><p class="eyebrow">CONSTRUCTION OPERATIONS & ASSET INTELLIGENCE</p><h1>Site Killick<span>Leave the site<br>at the site.</span></h1><p>Your people, your equipment, your peace of mind. Bring the working day together, without bringing it home.</p><div class="actions"><a class="button primary" href="/how-it-works">See how it works ${icon('arrow-up-right')}</a><a class="button" href="/pricing">Explore pricing ${icon('arrow-right')}</a></div></div><div class="hero-controls"><button id="skip-story" class="text-button">Skip story ${icon('arrow-down')}</button><label class="motion-control"><input id="reduced-motion" type="checkbox">Reduced motion</label></div><div class="construction-status"><div><span id="construction-stage">01 / 06 Excavation</span><span id="construction-percent">0%</span></div><progress id="construction-progress" max="1" value="0" aria-label="Construction progress"></progress></div></section></section>
  <section class="proof-strip"><div class="wrap"><span>ONE CONNECTED OPERATION</span>${['People & work', 'Safety & records', 'Tools & assets', 'Vehicles & equipment'].map(x => `<span>${icon('check')}${x}</span>`).join('')}</div></section>
  <section class="wrap band" id="story"><div class="section-heading"><div><p class="eyebrow">01 / THE WEIGHT OF RUNNING THE SITE</p><h2>The work ends.<br>The questions keep coming.</h2></div><p>Who has the missing tools? Are the hours right? Did that issue get resolved? When every answer depends on you, a good day can still follow you home.</p></div><div class="story-layout"><div class="story-index"><span id="story-number">01 / 03</span><h3 id="story-title">Every question finds you.</h3><p id="story-description">The crew needs answers. The office needs records. Your evening becomes the handover nobody planned.</p><div class="actions"><button class="icon-button" id="story-prev" aria-label="Previous story chapter" title="Previous chapter">${icon('arrow-left')}</button><button class="icon-button" id="story-next" aria-label="Next story chapter" title="Next chapter">${icon('arrow-right')}</button></div></div><div class="operation-flow" id="operation-flow"><div class="flow-heading"><span>END-OF-DAY HANDOVER</span><span id="flow-mode">Without a shared system</span></div><div class="flow-row"><span>${icon('receipt-text')}Disputed hours</span><strong data-owner="Payroll administrator">Business owner</strong></div><div class="flow-row"><span>${icon('wrench')}Missing tool</span><strong data-owner="Assigned lead hand">Business owner</strong></div><div class="flow-row"><span>${icon('shield-check')}Safety follow-up</span><strong data-owner="Safety administrator">Business owner</strong></div><div class="flow-row"><span>${icon('truck')}Service due</span><strong data-owner="Fleet manager">Business owner</strong></div><p id="flow-result">Four questions. One bottleneck.</p></div></div></section>
  <section class="soft-band"><div class="wrap band"><div class="section-heading"><div><p class="eyebrow">02 / A SYSTEM THAT SHARES THE LOAD</p><h2>Connected on site.<br>Settled off the clock.</h2></div><a class="text-button" href="/platform">Explore the platform ${icon('arrow-up-right')}</a></div><div class="pillar-grid">${[modules[0],modules[2],modules[3],modules[4]].map((m,i) => `<a class="pillar" href="/platform?module=${m.id}"><div class="pillar-top">${icon(m.icon)}<span>0${i+1}</span></div><h3>${['People & Work','Safety & Records','Tools & Assets','Vehicles & Equipment'][i]}</h3><p>${m.short}</p><span class="pillar-link">See the workflow ${icon('arrow-up-right')}</span></a>`).join('')}</div></div></section>
  <section class="wrap band"><div class="section-heading"><div><p class="eyebrow">03 / RECORDS, NOT REASSURANCE</p><h2>Ask to see the evidence.</h2></div><p>A useful demo starts with a real workflow. Follow the record from the first site event to the person who approves the next step.</p></div><div class="evidence-grid"><div>${icon('scan-line')}<h3>From tap to timesheet</h3><p>Follow an attendance event through supervisor review and payroll exceptions.</p></div><div>${icon('radio')}<h3>From tag to handoff</h3><p>Trace a tool's custody history and check hardware compatibility for your sites.</p></div><div>${icon('file-check')}<h3>From record to decision</h3><p>Review source evidence behind a daily summary. Your team retains final approval.</p></div></div><p class="small evidence-note">Pilot results and certification evidence will be published once verified. No performance statistics or certifications are claimed here.</p></section>${cta()}`;
}
export function bindHome() {
  const chapters = [
    ['Every question finds you.', 'The crew needs answers. The office needs records. Your evening becomes the handover nobody planned.', 'Without a shared system', 'Four questions. One bottleneck.'],
    ['Give every issue an owner.', 'Attendance, tool handoffs and safety records stay with the work. The next action goes to the responsible role.', 'With Site Killick', 'Clear responsibility. A record behind every action.'],
    ['Take home a quieter mind.', 'Your team reviews the details. You see the material exceptions, the supporting record and the decisions that need you.', 'Owner-level decisions only', 'Review what matters. Let the team handle the rest.'],
  ];
  let chapter = 0;
  const update = () => {
    const [title, description, mode, result] = chapters[chapter];
    document.querySelector('#story-number').textContent = `0${chapter+1} / 03`;
    document.querySelector('#story-title').textContent = title;
    document.querySelector('#story-description').textContent = description;
    document.querySelector('#flow-mode').textContent = mode;
    document.querySelector('#flow-result').textContent = result;
    document.querySelectorAll('[data-owner]').forEach(el => { el.textContent = chapter ? el.dataset.owner : 'Business owner'; });
    document.querySelector('#operation-flow').classList.toggle('connected', chapter > 0);
    document.querySelector('#story-prev').disabled = chapter === 0;
    document.querySelector('#story-next').disabled = chapter === 2;
  };
  document.querySelector('#story-prev').onclick = () => { chapter = Math.max(0, chapter - 1); update(); };
  document.querySelector('#story-next').onclick = () => { chapter = Math.min(2, chapter + 1); update(); };
  update();
}
export function platformPage() {
  const selected = new URLSearchParams(location.search).get('module');
  const active = modules.find(m => m.id === selected) || modules[0];
  return `<section class="page-intro wrap"><p class="eyebrow">THE PLATFORM / SIX CONNECTED MODULES</p><h1>Less chasing.<br>More clarity.</h1><p>One operating record for the people, work and equipment that keep your sites moving.</p></section><section class="wrap workspace-layout"><nav class="side-tabs" aria-label="Platform modules">${modules.map(m => `<button data-module="${m.id}" aria-pressed="${m.id === active.id}">${icon(m.icon)}${m.title}${icon('chevron-right')}</button>`).join('')}</nav><div id="module-content">${moduleContent(active)}</div></section><section class="wrap band"><div class="notice">${icon('info')}<span>These workflows describe the platform specification. Feature launch status and device compatibility will be confirmed during your demo.</span></div></section>${cta()}`;
}
function moduleContent(m) {
  return `<div class="module-heading"><span class="large-icon">${icon(m.icon)}</span><p class="eyebrow">${m.title.toUpperCase()}</p><h2>${m.short}</h2><p>${m.capability}</p></div><div class="feature-tags">${m.features.map(x => `<span>${icon('check')}${x}</span>`).join('')}</div><dl class="workflow-schema">${[['Operating problem',m.problem],['Site Killick capability',m.capability],['Required inputs',m.inputs],['Visible output',m.output],['Facilitated decision',m.decision],['User roles',m.roles]].map(([label,value],i) => `<div><dt><span>0${i+1}</span>${label}</dt><dd>${value}</dd></div>`).join('')}</dl><a class="text-button" href="/contact?topic=${encodeURIComponent(m.title)}">Discuss this workflow ${icon('arrow-up-right')}</a>`;
}
export function bindPlatform(refreshIcons) {
  document.querySelectorAll('[data-module]').forEach(button => { button.onclick = () => {
    const m = modules.find(m => m.id === button.dataset.module);
    document.querySelectorAll('[data-module]').forEach(b => b.setAttribute('aria-pressed', String(b === button)));
    document.querySelector('#module-content').innerHTML = moduleContent(m);
    history.replaceState({}, '', `/platform?module=${m.id}`);
    refreshIcons();
  }; });
}
export function rolesPage() {
  const i = Math.max(0, roles.findIndex(r => r.name === new URLSearchParams(location.search).get('role')));
  return `<section class="page-intro wrap"><p class="eyebrow">HOW IT WORKS / EVERY ROLE HAS A PART</p><h1>A clearer day.<br>For the whole crew.</h1><p>Follow the workflow that fits your responsibilities, from the first clock-in to the final approval.</p></section><section class="wrap workspace-layout"><nav class="side-tabs" aria-label="Role guides">${roles.map((r,n) => `<button data-role="${n}" aria-pressed="${i===n}">${icon(r.icon)}${r.name}${icon('chevron-right')}</button>`).join('')}</nav><div id="role-content">${roleContent(roles[i])}</div></section>${cta()}`;
}
function roleContent(r) {
  return `<div class="module-heading"><span class="large-icon">${icon(r.icon)}</span><p class="eyebrow">${r.name.toUpperCase()}</p><h2>${r.goal}</h2></div><div class="comparison"><div><p class="eyebrow">THE TRADITIONAL DAY</p><p>${r.before}</p></div><div><p class="eyebrow">WITH SITE KILLICK</p><p>${r.after}</p></div></div><h3 class="subheading">Your first seven steps</h3><ol class="onboarding">${r.steps.map(s => `<li>${s}</li>`).join('')}</ol><h3 class="subheading">A day in the role</h3><div class="daily-routine">${r.daily.map((s,i) => `<div><span>${['START OF SHIFT','DURING THE DAY','BEFORE YOU LEAVE'][i]}</span><p>${s}</p></div>`).join('')}</div><div class="tutorial-heading"><h3>Learning library</h3><span class="badge">Text guides available</span></div><p class="small">Video tutorials are awaiting approved recordings and captions.</p>${r.daily.map((s,i) => `<details><summary>Chapter ${i+1}: ${s}</summary><p>Start with your assigned site and the source record. ${s}. Check the timestamp, responsible person and supporting evidence. Record any correction and route unresolved questions to the assigned supervisor before closing the item.</p></details>`).join('')}<a class="text-button role-cta" href="/contact?topic=${encodeURIComponent(r.name)}">Walk through this role with us ${icon('arrow-up-right')}</a>`;
}
export function bindRoles(refreshIcons) {
  document.querySelectorAll('[data-role]').forEach(button => { button.onclick = () => {
    const role = roles[Number(button.dataset.role)];
    document.querySelectorAll('[data-role]').forEach(b => b.setAttribute('aria-pressed', String(b === button)));
    document.querySelector('#role-content').innerHTML = roleContent(role);
    history.replaceState({}, '', `/how-it-works?role=${encodeURIComponent(role.name)}`);
    refreshIcons();
  }; });
}
export function aboutPage() {
  return `<section class="page-intro wrap"><p class="eyebrow">ABOUT SITE KILLICK / AN ANCHOR FOR THE WORKING DAY</p><h1>Built around work.<br>And life beyond it.</h1><p>A construction business should give its people a future, without taking every evening in return.</p></section><section class="wrap about-story"><div class="anchor-mark" aria-hidden="true">${icon('anchor')}<span>ROOTED IN ATLANTIC CANADA</span></div><div><p class="eyebrow">WHY KILLICK?</p><h2>A steady point<br>in a moving world.</h2><p>A killick is a traditional Atlantic Canadian anchor, made with stone and timber. Simple materials, brought together for one purpose: holding steady.</p><p>That idea shapes Site Killick. We connect the everyday records that help a construction business hold its course: people, work, safety, tools and equipment.</p><p>The mission is to reduce owner dependency and give teams the context to act, so founders can leave work with the people trusted to run it.</p></div></section><section class="soft-band"><div class="wrap band"><p class="eyebrow">OUR OPERATING PRINCIPLES</p><h2>Trust starts with a clear record.</h2><div class="evidence-grid"><div>${icon('file-check')}<h3>Evidence before claims</h3><p>Product workflows should be demonstrated with source records. Pilot outcomes are published only after verification.</p></div><div>${icon('user-check')}<h3>People retain authority</h3><p>AI can prepare summaries and recommendations. Human supervisors review the evidence and retain approval authority.</p></div><div>${icon('eye')}<h3>Clarity before noise</h3><p>Routine work belongs with the responsible team. Escalations should explain what needs attention and why.</p></div></div></div></section><section class="wrap band"><div class="section-heading"><div><p class="eyebrow">SECURITY, ASSURANCE & HARDWARE</p><h2>Know what you are relying on.</h2></div><p>Ask for the evidence relevant to your procurement process. Production controls and commercial commitments must be confirmed before rollout.</p></div>${[['Security & data residency','Encryption in transit and at rest, access controls, retention and regional hosting are production requirements. Hosting locations and audit reports are not yet published.','security'],['SOC 2 roadmap','Independent assurance is a roadmap item. This website does not claim SOC 2 certification or a completed compliance audit.','security'],['Responsible AI','Generated summaries and extracted document fields require human review. They do not replace professional, payroll or safety judgment.','acceptable-use'],['Hardware & insurance','Confirm device compatibility, warranty coverage and replacement terms in your order. Any insurance eligibility must be verified with the underwriter.','hardware']].map(([h,p,slug]) => `<div class="trust-row"><h3>${h}</h3><div><p>${p}</p><a href="/legal/${slug}">Read the policy ${icon('arrow-up-right')}</a></div></div>`).join('')}</section>${cta()}`;
}
export function legalPage(slug) {
  const policy = policies.find(p => p.slug === slug);
  if (slug && !policy) return notFound();
  return `<section class="page-intro wrap"><p class="eyebrow">LEGAL & TRUST / POLICY LIBRARY</p><h1>${policy ? policy.title : 'Clear terms.<br>In one place.'}</h1><p>${policy ? policy.summary : 'Our approach to privacy, responsible use, accessibility and hardware support.'}</p></section><section class="wrap legal-layout"><nav class="policy-nav" aria-label="Legal policies"><a href="/legal" ${!slug ? 'aria-current="page"' : ''}>Policy overview</a>${policies.map(p => `<a href="/legal/${p.slug}" ${slug===p.slug ? 'aria-current="page"' : ''}>${p.title}</a>`).join('')}</nav><article class="legal-content"><div class="notice">${icon('file-clock')}<span><strong>Draft for review.</strong> These pages describe the preview and proposed approach. Approved legal policies and contracting details are required before launch.</span></div>${policy ? `<p class="small">Document status: pre-launch draft &middot; September 2026</p>${policy.sections.map(([h,p],i) => `<section><h2>${i+1}. ${h}</h2><p>${p}</p></section>`).join('')}${slug === 'cookies' ? '<button class="button" data-cookie-settings>Manage cookie preferences</button>' : ''}<a class="text-button" href="/contact?topic=${slug === 'security' ? 'Security' : slug === 'accessibility' ? 'Accessibility' : 'Policy question'}">Ask a question ${icon('arrow-up-right')}</a>` : `<div class="policy-list">${policies.map(p => `<a href="/legal/${p.slug}"><div><h2>${p.title}</h2><p>${p.summary}</p></div>${icon('arrow-up-right')}</a>`).join('')}</div>`}</article></section>`;
}
export function signInPage(appUrl) {
  return `<section class="wrap page-intro"><p class="eyebrow">SITE KILLICK / WORKSPACE ACCESS</p><h1>Your working day<br>starts here.</h1><p>${appUrl ? 'Continue to your Site Killick workspace.' : 'Workspace sign-in will be available when the production application is connected. Existing teams should use the workspace link provided by their administrator.'}</p><a class="button primary" href="${appUrl ? escapeHtml(appUrl) : '/contact?topic=Workspace%20access'}">${appUrl ? 'Open workspace' : 'Request workspace access'} ${icon('arrow-up-right')}</a></section>`;
}
export function notFound() { return `<section class="wrap page-intro"><p class="eyebrow">404 / PAGE NOT FOUND</p><h1>A little off site.</h1><p>This address does not match a page. Head back to the main site to continue.</p><a class="button primary" href="/">Back to Site Killick ${icon('arrow-right')}</a></section>`; }
